# footprint-em-calculator

run frontend part dev mode

`cd frontend`

`yarn install` or `npm i`

`yarn start` or `npm run start`

run server part dev mode

`cd server`

`yarn install` or `npm i`

`yarn start:dev` or `npm run start:dev`
